<view class="l_container">
    <map id="map" longitude="{{longitude}}" latitude="{{latitude}}" 
    scale="14" markers="{{markers}}"
    bindmarkertap="markertap"
    bindregionchange="regionchange"
    show-location="show">
    </map>
    <button class="nav_btn" type="primary" size="mini"  bindtap="navigate"><text>导航</text></button>
  </view>